// A copy of the array with true students
const trueStudent = students.map(student => student)

console.log(trueStudent);

// A copy of the array with false names
const falseStudent = students.map(student => student)

console.log(falseStudent);